# Codex Watchdog v0.1

- [x] Inspect existing source, installed CLI 0.154.0 help and generated protocol schemas.
- [ ] Implement .NET 10 configuration, bounded state machine and conservative safety policy.
- [ ] Implement daemon proxy / loopback WebSocket session adapter, explicit session selection and single-writer lock.
- [ ] Add mock app-server, tests, build and exercise CLI, cancellation and dry-run.
- [ ] Publish Windows executable and document actual verification limits.

Transport: initialize, initialized, thread/loaded/list, thread/read,
thread/turns/list (latest full turn), turn/start. No terminal prompt heuristics.
